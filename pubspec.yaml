name: toddler_logic
description: Local-first cognitive development app for toddlers.
publish_to: 'none'

version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  bloc: ^9.0.0
  hive: ^2.2.3
  meta: ^1.16.0

dev_dependencies:
  build_runner: ^2.4.11
  hive_generator: ^2.0.1
  lints: ^5.0.0
  mocktail: ^1.0.4
  test: ^1.25.8
